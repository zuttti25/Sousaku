<% if messages.present? %>
<% messages.each do |m| %>
<strong><%= m.message %></strong>
<%= link_to user_path(m.user.id) do %>
<small>by<%= m.user.name %>さん</small>
<% end %>

<hr>
<% end %>
<% else %>
<h3 class="text-center">メッセージはまだありません</h3>
<% end %>