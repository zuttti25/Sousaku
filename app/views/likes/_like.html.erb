   <p>いいね件数: <%= post.likes.count %></p>
   <% if post.liked_by?(current_user) %>
   <%= link_to 'いいねを取り消す', post_likes_path(post), method: :delete, remote: true %>
   <% else %>
   <%= link_to 'いいね', post_likes_path(post), method: :post, remote: true %>
   <% end %>