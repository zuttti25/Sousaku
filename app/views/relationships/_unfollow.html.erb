 <%= form_with model: current_user.relationships.find_by(follow_id: @user.id), html: { method: :delete }, remote: true do |f| %>
 <%= hidden_field_tag :follow_id, @user.id %>
 <%= f.submit 'unfollow', class: 'btn btn-nav6' %>
 <% end %>